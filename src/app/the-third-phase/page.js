import TextTitle from '../../components/TextTitle'

const TheThirdPhase = () => {
  return (
    <div style={{
      height: '100vh'
    }}>
          <TextTitle title={"This section will launch alongside the third phase!"} />
    </div>
  );
};

export default TheThirdPhase;
