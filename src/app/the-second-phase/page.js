import TextTitle from '../../components/TextTitle'

const TheSecondPhase = () => {
  return (
    <TextTitle title={"Exciting updates ahead! Phase 2 is launching in just one month!"} />
  );
};

export default TheSecondPhase;
